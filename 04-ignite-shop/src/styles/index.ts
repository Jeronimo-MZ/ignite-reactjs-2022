import { createStitches } from "@stitches/react";

export const {
    config,
    styled,
    createTheme,
    theme,
    css,
    globalCss,
    getCssText,
    keyframes,
} = createStitches({
    theme: {
        colors: {
            rocketseat: "#8257e6",
        },
    },
});
